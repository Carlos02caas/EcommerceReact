import React from 'react';
import { Layout } from '../../components/Layout';
import { Card } from '../../components/Card';

const Store = () => {
    return (
        <Layout>
            Store Section
            <Card/>
        </Layout>
    );
}

export { Store };
